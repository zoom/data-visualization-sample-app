<h1>{{appDisplayName}}</h1>
<p>Configure which of your Zoom Account Activity data to display</p>

<div class="container" style="margin-top:20px;">
	<div class="row">
        <div class="col-xs-6">
            <h3 class="text-center">Select Activities</h3>
            <div class="well" style="max-height: 300px;overflow: auto;">
        		<ul class="list-group checked-list-box">
				{{#if configurationOptionsByScope}}
				  {{#each configurationOptionsByScope}}
                  <li class="list-group-item id="{{dataId}}">{{displayName}}</li>
				  {{/each}}
				{{/if}}
                </ul>
            </div>
        </div>
    </div>
</div>
